# Invalid: top-level `schema_version` is missing.
description: "schema with no version declaration"
layouts:
  - name: only
    endianness: little
    match:
      offset: 0
      bytes: [0xAA]
    min_payload_bytes: 4
    fields:
      - name: counter
        offset: 0
        encoding: uint32
        size_bytes: 4
