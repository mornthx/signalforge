# M20 Plan

1. Expand token source + schema from light-only to M20 themes.
2. Update generator outputs for theme-specific QSS and C++/Python tokens.
3. Implement runtime theme application in `SignalForgeStyle`.
4. Add `MainWindow` theme menu, shortcuts, tab-order path, and visual focus
   harness hook.
5. Make chart host/scene colors follow the active palette.
6. Add visual tests and committed baselines for dark, high-contrast, and
   focus-ring states.
7. Verify generator freshness, build, focused visual tests, and diff hygiene.
